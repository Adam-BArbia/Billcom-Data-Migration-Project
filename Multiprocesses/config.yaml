mysql:
  host: "localhost"
  user: "root"
  password: "adam@KIRA98"
  database: "email_recavaring"
  input: custemer_source

logging:
  config_file: "logs/logging_config.yaml"

paths:
  #sql_script: "MAIN TABLE.sql"
  csv_output_directory: "directory/"

default:
  email: "default@def.df"
  phone_number: "00000000"
  dob: "1970-01-01"

etl:
  chunk_number: 100
  processes: 3

input_directory: directory/
output_cvs: 'filtered_output.csv'